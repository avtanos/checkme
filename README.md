# Карта провайдеров услуг Кыргызстана

Веб-приложение для поиска грузовых машин, сантехников, эвакуаторов и других провайдеров услуг на интерактивной карте Кыргызстана. Карта по умолчанию показывает Бишкек.

## Возможности

- 🗺️ Интерактивная карта с отображением провайдеров услуг
- 🔍 Фильтрация по категориям
- 📋 Список провайдеров с контактами
- 📝 Форма отправки сообщений провайдерам
- 👤 Личный кабинет провайдера для управления данными
- 📱 Адаптивный дизайн для мобильных устройств

## Технологии

- **Backend**: FastAPI (Python)
- **Frontend**: React
- **Карта**: Leaflet
- **База данных**: SQLite

## Установка и запуск

### Backend

```bash
cd backend
pip install -r requirements.txt
uvicorn main:app --reload
```

Backend будет доступен по адресу: http://localhost:8000
API документация: http://localhost:8000/docs

### Frontend

```bash
cd frontend
npm install
npm start
```

Frontend будет доступен по адресу: http://localhost:3000

## Структура проекта

```
.
├── backend/          # FastAPI приложение
│   ├── main.py      # Основной файл API
│   ├── models.py    # Модели базы данных
│   ├── schemas.py   # Pydantic схемы
│   └── database.py  # Настройка БД
├── frontend/        # React приложение
│   ├── src/
│   │   ├── pages/   # Страницы приложения
│   │   └── components/ # React компоненты
│   └── public/
└── README.md
```

## API Endpoints

- `GET /api/providers` - Получить список провайдеров
- `GET /api/providers/{id}` - Получить провайдера по ID
- `POST /api/providers` - Создать нового провайдера
- `PUT /api/providers/{id}` - Обновить провайдера
- `DELETE /api/providers/{id}` - Удалить провайдера
- `POST /api/providers/{id}/messages` - Отправить сообщение
- `GET /api/providers/{id}/messages` - Получить сообщения провайдера
- `GET /api/categories` - Получить список категорий

## Использование

1. Запустите backend и frontend
2. Откройте http://localhost:3000 в браузере
3. На карте отобразятся все доступные провайдеры
4. Кликните на маркер или элемент списка для просмотра деталей
5. Отправьте сообщение провайдеру через форму
6. Для управления данными провайдера откройте кабинет по ссылке `/cabinet/{id}`

